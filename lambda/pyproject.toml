[project]
name = "ffsync-lambda-handler"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "aws-lambda-powertools==3.34.0",
    "boto3==1.43.89",
    "cryptography==50.0.1",
    "pydantic==2.13.5",
    "mohawk==1.1.0",
    "PyJWT==2.13.0",
    "requests==2.34.2"
]


[dependency-groups]
dev = [
    "pytest==9.1.1",
    "pytest-cov==7.1.0",
    "pytest-xdist==3.8.0",
    "black==26.5.1",
    "isort==8.0.1",
    "flake8==7.3.0",
    "Flake8-pyproject==1.2.4",
    "mypy==2.3.1",
    "types-boto3==1.43.88",
    "hypothesis>=6.0.0",
    "types-requests==2.33.0.20260712",
    "datamodel-code-generator==0.76.2",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = [
    "--verbose",
    "--cov=src",
    "--cov-report=term-missing",
    "--cov-report=term",
    "--cov-report=html",
    "--cov-report=xml",
    "--cov-fail-under=100",
    "-n=auto"
]

[tool.coverage.run]
source = ["src"]
omit = [
    "*/tests/*",
    "*/test_*",
    "*/generated/*",
]
branch = true
parallel = true
relative_files = true

[tool.coverage.report]
exclude_lines = [
    "pragma: nocover",
    "def __repr__",
    "raise AssertionError",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
    "if TYPE_CHECKING:",
    "@abstractmethod",
    "pass",
]

[tool.black]
line-length = 100
target-version = ["py314"]
src = ["src/", "tests/"]

[tool.isort]
profile = "black"
src_paths = ["src/", "tests/"]
line_length = 100
# Relative src_paths above are anchored to isort's `directory`, which datamodel-codegen
# overrides to the codegen output dir -- they then miss, `src` looks third-party, and
# generated/ loses the blank line before its first-party imports. Pin it here instead.
# No trailing slash: a known_* entry ending in "/" is os.listdir()-expanded and will crash.
known_first_party = ["src"]

[tool.flake8]
max-line-length = 100
extend-ignore = [
    "E203",  # whitespace before ':'
    "E266",  # too many leading '#' for block comment
    "E501",  # line too long (handled by black)
    "W503",  # line break before binary operator
    "W504",  # line break after binary operator
]
exclude = [
    ".git",
    "__pycache__",
    "build",
    "dist",
    ".venv",
    "venv",
    ".eggs",
    "*.egg-info",
    ".pytest_cache",
    ".mypy_cache",
    ".coverage",
    "htmlcov",
]
per-file-ignores = [
    "__init__.py:F401",  # imported but unused
    "tests/*:S101",      # use of assert detected (if using flake8-bandit)
    "src/shared/models.py:F401", # importing generated models for re-export
    "src/shared/generated/*:F722", # constr field constraints
]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "C90", # mccabe complexity
]

[tool.mypy]
check_untyped_defs = true
show_error_codes = true
pretty = true
ignore_missing_imports = true
files = ["src/", "tests/"]
exclude = [
    "__pycache__",
    ".pytest_cache",
    ".mypy_cache",
    "htmlcov",
]
plugins = ["pydantic.mypy"]
